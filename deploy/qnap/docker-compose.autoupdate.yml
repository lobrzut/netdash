# yaml-language-server: $schema=./qnap-compose.schema.json
# Watchtower bez profilu — dla Container Station (Import drugiego pliku lub merge w UI)

version: "2.4"

services:
  watchtower:
    profiles: []
