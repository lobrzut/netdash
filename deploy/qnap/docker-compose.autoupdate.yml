# yaml-language-server: $schema=https://json.schemastore.org/docker-compose.json
# Watchtower bez profilu — dla Container Station (Import drugiego pliku lub merge w UI)

version: "2.4"

services:
  watchtower:
    profiles: []
